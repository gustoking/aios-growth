{
  "name": "aios-growth",
  "version": "1.0.0",
  "description": "AIOS Product Growth Case Study",
  "private": true,
  "scripts": {
    "dev": "npx serve .",
    "build": "echo 'Static HTML project - no build required'",
    "lint": "echo 'No linter configured'"
  },
  "dependencies": {
    "@vercel/analytics": "^2.0.1"
  }
}
